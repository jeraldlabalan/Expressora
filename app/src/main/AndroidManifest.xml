<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allow app to run on devices with or without camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Expressora">


        <!--        Authentication-->
        <activity
            android:name=".auth.ResetPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />


        <!--        User Side-->
        <activity
            android:name=".dashboard.user.learn.LearnActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.user.quiz.QuizActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.user.translation.TranslationActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.user.notification.NotificationActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.user.community_space.CommunitySpaceActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.user.tutorial.TutorialActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.user.settings.SettingsActivity"
            android:exported="false" />


        <!--        FOR STARTING THE SPECIFIC FILE-->
        <activity
            android:name=".dashboard.admin.settings.AdminSettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!--        Admin Side-->
        <activity
            android:name=".dashboard.admin.notification.NotificationActivity"
            android:exported="false" />
        <!--        <activity-->
        <!--            android:name=".dashboard.admin.settings.AdminSettingsActivity"-->
        <!--            android:exported="false" />-->
        <activity
            android:name=".dashboard.admin.tutorialmonitoring.TutorialMonitoringActivity"
            android:exported="false" />


        <!--        Splash Screen-->
        <activity
            android:name=".home.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Expressora">
            <!--            <intent-filter>-->
            <!--                <action android:name="android.intent.action.MAIN" />-->

            <!--                <category android:name="android.intent.category.LAUNCHER" />-->
            <!--            </intent-filter>-->
        </activity>
    </application>

</manifest>